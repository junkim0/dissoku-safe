/** @type {import('next').NextConfig} */
const nextConfig = {
  // This file is added to force Vercel to re-evaluate the project structure
  // and hopefully clear the stuck deployment cache.
};

export default nextConfig; 